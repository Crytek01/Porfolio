import styled from 'styled-components';

export const ExternalLink = styled.a`
  &:visited,
  &:link {
    color: #6e9cf8;

    text-decoration: none;
    cursor: pointer;
  }

  &:hover,
  &:active {
    color: #487be0;
  }
`;
